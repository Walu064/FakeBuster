def filter_by_query(words : str, query : str) -> bool:
    query = query.split(" ")
    if len(query) == 0:
        return True

    # TODO: Keywords filtering
    # Check if ad's keywords suit to query
    
    return True


def get_keywords(text : str) -> list[str]:
    keywords : list[str] = []
    
    # TODO: Get keywords from raw ad text
    
    return keywords